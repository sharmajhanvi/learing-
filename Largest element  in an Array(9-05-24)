import java.util.*;
public class Main
{
	public static void main(String[] args) {
	    int arr[]={5,8,9,6,5,3,1};
	    int max=Integer.MIN_VALUE;
	    for(int i=0;i<arr.length;i++){
	        max=Integer.max(max,arr[i]);
	    }
		System.out.println(max);
	}
}
